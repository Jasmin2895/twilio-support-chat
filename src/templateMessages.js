let date = new Date();

let dummyMessages = [
  {
    body:
      "This is an automated notification. You will get updates as soon your processed.",
    phoneNumber: "",
    notification: 1,
    timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    time: 1,
  },
  {
    body:
      "This is an automated notification. You will get updates as soon your processed.",
    phoneNumber: "",
    notification: 2,
    timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    time: 2,
  },
  {
    body:
      "This is an automated notification. You will get updates as soon your processed.",
    phoneNumber: "",
    notification: 3,
    timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    time: 3,
  },
  {
    body:
      "This is an automated notification. You will get updates as soon your processed.",
    phoneNumber: "",
    notification: 4,
    timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    time: 4,
  },
  {
    body:
      "This is an automated notification. You will get updates as soon your processed.",
    phoneNumber: "",
    notification: 5,
    timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
    time: 5,
  },
];

export default dummyMessages;
